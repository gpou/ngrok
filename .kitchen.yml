---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04

suites:
  - name: default
    run_list:
      - recipe[ngrok::default]
    attributes:
      ngrok:
        authtoken: Dtv-m6wlNK8DIzE8UB9u
        tunnels:
          kitchen-ci-test-vm:
            proto:
              http+https: 80
